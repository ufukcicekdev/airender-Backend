# Railway — Celery beat (optional separate service)
# Dashboard: Root Directory = backend, Config file = railway.celery-beat.toml

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "celery -A config beat -l info"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
