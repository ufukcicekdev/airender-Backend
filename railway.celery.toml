# Deprecated: use railway.toml on all backend services + APP_PROCESS=celery
# Kept so old Railway links still work.

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "sh scripts/railway-start.sh"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
